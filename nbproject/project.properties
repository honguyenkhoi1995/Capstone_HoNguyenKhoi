build.classes.dir=${build.web.dir}/WEB-INF/classes
build.dir=build
build.generated.dir=${build.dir}/generated
build.web.dir=${build.dir}/web
dist.dir=dist
dist.jar=${dist.dir}/${ant.project.name}.war
javac.source=1.8
javac.target=1.8
src.dir=src/java
web.dir=Web Pages
